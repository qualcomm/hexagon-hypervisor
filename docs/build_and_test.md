
# Building and Testing

The typical workflow is to build first, then run tests:

```
make opt       # build the optimized variant (default ARCHV=81)
make test      # compile and run all tests, produce test.out summary
```

Or combined: `make opt test`

No `-j` flag is needed — the top-level `makefile` sets `MAKEFLAGS += -j$(nproc)` automatically.
Override with `make -j8` (or any number) if you need to limit parallelism on a shared machine.

`make opt` builds with optimizations enabled.  `make ref` attempts to use
reference versions of implementations that might be available rather than the
optimized ones.  We want to test both opt and ref implementations to ensure they
are kept in sync.

`make test` will build test binaries and run them.  If the `install/` directory
does not exist yet, it will auto-build using the `ref` model.  To test the `opt`
build, always run `make opt` (or `make ref`) before `make test`.

By setting the ARCHV variable on the make command line, like `ARCHV=68` we can
change the architecture that we build for.  When the project started there were
substantial changes between architectures that affected monitor mode
and how code was optimized.

Some architectures are similar enough that we can reuse builds for multiple versions.
The primary ones that we want to consider for now:

* 65
* 68
* 73
* 81

however this is likely to grow to more architectures.

# Output Files

`test.out` — written fresh on each `make test` run.  Contains a one-line
`SUMMARY: N passed, M failed` followed by per-test PASS/FAIL detail from
`check-fail`.  This is the first file to look at after a test run.

`make.log` — raw interleaved output from the parallel test run (build +
simulator output for all tests combined).  Useful for diagnosing build failures
that prevent tests from compiling.  Each individual test also writes its own
`<test-dir>/make.log` with the full output for just that test — look there when
a specific test fails.

`build.log` — not generated automatically; capture it with
`make -j16 opt 2>&1 | tee build.log` when you need to review build warnings.

`h2_report.html` / `report.html` — HTML summary of PASS/FAIL per test,
generated by `make test`.

# Artifacts

By default we compile files in-place, and artifacts in "install" after they are
built and collected.

In the `install/bin` directory is `booter`, our binary that loads up other programs 
into their own VM to execute.  This is the primary workhorse for running tests.

In the `install/lib` directory there is `libh2.a` which contains helper functions for
user/guest programs, as well as `libh2kernel.a` which contains most of the code 
and data for monitor mode execution.

Unsuprisingly, `install/include` contains header files.

`install/ver` is a helpful file including version information.

`install/src` contains some files that are often needed for building programs,
such as `min_crt0.S`, `bootvm.S`, and `bootvm_entry.S`.

`install/scripts` contains configuration files and scripts, including linker
scripts (`vapa_bootvm.x`, `vapa_kernel.x.in`) and simulator device configs
(`devsim_v*.cfg`).

`install/stake` contains build support files used to compute kernel sizing.

# Build and Test todo

* [x] Build out of tree: Put all artifacts into a separate directory, per-ARCHV verison and per other configuration option, 
  so that all the various builds and tests can be executed in parallel without files being in conflict and overwritten.
* [x] Fix ref library archive paths: `kernel/build/ref/libh2kernel.a` and `libs/h2/build/ref/libh2.a` are shared across
  all ARCHV ref builds. Rename to `build/ref_v$(ARCHV)/` to match the opt pattern and allow parallel ref builds.
* [x] Isolate generated files (e.g. `asm_offsets.h`): Generated headers land in a single shared `kernel/include/` regardless
  of ARCHV/variant. The generating binary is already in the isolated BUILD_DIR; the output needs to go to a per-ARCHV location
  (e.g. into BUILD_DIR or INSTALLPATH) rather than the shared include staging area.
* [x] Fix `libs/Makefile` tmp directory: The `libs/tmp/` directory used to assemble the combined `libh2.a` is not
  ARCHV/variant-isolated. Needs a per-ARCHV/variant path before parallel lib builds are safe.
* [x] Isolate test build artifacts: Tests currently build (object files, ELFs, logs) in-source alongside the test source
  files. These should land in a per-ARCHV/variant output directory to allow parallel multi-arch test runs without conflicts.
* [ ] Unify all build artifacts under `artifacts/v$(ARCHV)/$(T)/build/`: Currently kernel, libs, booter, qurt, and angel
  each have their own in-source or scattered build directories (`kernel/build/obj/`, `libs/h2/build/obj/`, `libs/tmp/`,
  `booter/*.o`, etc.).  Move all intermediate build artifacts under `artifacts/v$(ARCHV)/$(T)/build/<component>/`
  using INSTALLPATH as the anchor: `BUILD_DIR := $(patsubst %/install,%/build/kernel,$(INSTALLPATH))`.
  This makes clean trivial (`rm -rf artifacts/v$(ARCHV)/$(T)`), naturally isolates parallel ARCHV builds for all
  components including booter/qurt/angel, and is consistent with how test artifacts are already handled.
  See "Unified Build Directory Plan" in notes.md for full details and the list of files to change.
* [ ] Allow for easy creation of a new build variant (ref, opt, or other tweak) or new architecture version.  Document how this is done.
* [ ] Enable Parallel Build and Test: once there is no conflict between different builds, all the builds and testing can run in parallel
* [ ] Single artifact directory: Create a single tree with all configurations and versions.  For example, instead of one `install` directory 
  that has the artifacts for the current configuration, we might have `artifacts/v68/opt/install` 
* [ ] `make test` in the main project directory should build and test all configurations (but can spawn scripts / submakes)
* [ ] On a `make test`, collect all the quality metrics into a single report under the artifacts directory

