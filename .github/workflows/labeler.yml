name: "Pull Request Labeler"

on:
- pull_request_target

jobs:
  labeler:
    name: Label pull requests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
    - uses: actions/labeler@v6
