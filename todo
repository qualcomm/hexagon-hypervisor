
General:

* Move external guests out to other repositories
* Possibly move all guests to other repositories, or at least other root directories
* We should have the notion of a release / release branch

H2 infrastructure:
* Update documentation. Switch to doxygen or other in-file documentation scheme?
* Go back over tests, get to ~100% unit test coverage
* Refactor & optimize translation
* Even better interrupt mechanism -- interrupts delivered to pool of waiting threads
* Power Collapse
* Island mode
* Test 36 bit PA bits more
* Kernel trace, or just continue to use ETM?
* Continue to eliminate global data where possible
* Refactor vector context save/restore (?)  Maybe move to new module?
* V6x PMU work (now w/ STID)
* Low: split bulk context storage from kernel info to split between DDR & TCM/L2locked
* optimize, get code size down.  Code size + data size < 24KB.
* Better page table format, better storage efficiency but faster than linear.

VMM tasks:

* Update VMM docs
* Page tables w/ 36-bit+ PA, possibly multilevel?
* Secure Debug

DONE : * Switch to LLVM, get LLVM guys to make global reg work better
DONE : * (I think) Make sure all pa values are 36-bit clean
DONE : * (I think) Make sure extra PA bits are used as appropriate (e.g. futex; everywhere we use pa_t)
DONE : * Save/restore framelimit,framekey
DONE : * Save/restore pktcount
DONE : * Try and optimize context storage? -- changed to 16 per-cpu interrupts

